<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  <a href="/">HOME</a>
  <h1>MEMBER</h1>
  <ul>
    <% if(MEMBERS.length !== 0) { %>
    <% for (let i = 0; i < MEMBERS.length; i++) { %>
    <ol>
      <li>id : <%= MEMBERS[i].id %></li>
      <li>password : <%= MEMBERS[i].password %></li>
    </ol>

    <% } %>
    <% } else { %>
    <p>회원 데이터가 없습니다.</p>
    <% } %>

  </ul>
</body>

</html>